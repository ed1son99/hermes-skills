interface:
  display_name: "tools-deps-audit"
  short_description: "依赖审计和安全分析"
  default_prompt: "Use $tools-deps-audit to help with tools deps audit tasks."
policy:
  allow_implicit_invocation: true
