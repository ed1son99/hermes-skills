interface:
  display_name: "tools-deps-upgrade"
  short_description: "依赖升级策略"
  default_prompt: "Use $tools-deps-upgrade to help with tools deps upgrade tasks."
policy:
  allow_implicit_invocation: true
