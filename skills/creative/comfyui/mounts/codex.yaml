interface:
  display_name: "comfyui"
  short_description: "Generate images, video, and audio with ComfyUI — install, launch, manage nodes/models, run workflows with parameter injection. Uses the official comfy-cli for lifecycle and direct REST/WebSocket API for execution."
  default_prompt: "Use $comfyui to help with comfyui tasks."
policy:
  allow_implicit_invocation: true
