interface:
  display_name: "imessage"
  short_description: "Send and receive iMessages/SMS via the imsg CLI on macOS."
  default_prompt: "Use $imessage to help with imessage tasks."
policy:
  allow_implicit_invocation: true
