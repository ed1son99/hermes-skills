interface:
  display_name: "apple-notes"
  short_description: "Manage Apple Notes via memo CLI: create, search, edit."
  default_prompt: "Use $apple-notes to help with apple notes tasks."
policy:
  allow_implicit_invocation: true
