interface:
  display_name: "simplify-code"
  short_description: "Parallel 3-agent cleanup of recent code changes."
  default_prompt: "Use $simplify-code to help with simplify code tasks."
policy:
  allow_implicit_invocation: true
