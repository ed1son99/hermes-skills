interface:
  display_name: "llama-cpp"
  short_description: "llama.cpp local GGUF inference + HF Hub model discovery."
  default_prompt: "Use $llama-cpp to help with llama cpp tasks."
policy:
  allow_implicit_invocation: true
