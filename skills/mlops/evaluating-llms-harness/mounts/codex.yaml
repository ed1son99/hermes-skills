interface:
  display_name: "evaluating-llms-harness"
  short_description: "lm-eval-harness: benchmark LLMs (MMLU, GSM8K, etc.)."
  default_prompt: "Use $evaluating-llms-harness to help with evaluating llms harness tasks."
policy:
  allow_implicit_invocation: true
