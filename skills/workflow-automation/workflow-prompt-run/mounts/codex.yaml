interface:
  display_name: "workflow-prompt-run"
  short_description: "并行或顺序执行多个 Prompt"
  default_prompt: "Use $workflow-prompt-run to help with workflow prompt run tasks."
policy:
  allow_implicit_invocation: true
