interface:
  display_name: "sc-index-repo"
  short_description: "仓库索引 — 94% token 减少 (58K → 3K)"
  default_prompt: "Use $sc-index-repo to help with sc index repo tasks."
policy:
  allow_implicit_invocation: true
